"""
【盛最多水的容器 - 双指针】

思路（简洁）：
左右指针分别指向数组两端，计算当前面积 = 较短高度 × 两指针距离。
每次移动较短的指针（因为面积由短板决定，移动短板可能增大面积），
更新最大值，直到两指针相遇。

易错点：
1. 面积用较短的高度乘宽度。
2. 当 height[l] < height[r] 时移动 l，否则移动 r。
3. 输入空数组或长度为1时返回0（循环条件自动处理）。

时空复杂度：
- 时间 O(n)，每个元素访问一次
- 空间 O(1)

输入输出示例：
示例 1：
输入: [1,8,6,2,5,4,8,3,7]
输出: 49

示例 2：
输入: [1,1]
输出: 1
"""

import sys
import json
from typing import List

class Solution:
    def maxArea(self, height: List[int]) -> int:
        ans = 0
        l, r = 0, len(height) - 1
        while l < r:
            if height[l] < height[r]:
                cur = height[l] * (r - l)
                l += 1
            else:
                cur = height[r] * (r - l)
                r -= 1
            ans = max(ans, cur)
        return ans

def main():
    data = sys.stdin.read().strip()
    if not data:
        return
    try:
        height = json.loads(data)
    except:
        height = list(map(int, data.split()))
    sol = Solution()
    print(sol.maxArea(height))

if __name__ == "__main__":
    main()
